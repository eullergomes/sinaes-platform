version: '3.8'
services:
  mongo:
    image: mongo:latest
    container_name: mongodb_local
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    command: mongod --replSet rs0 --bind_ip localhost,mongo
volumes:
  mongo-data:
